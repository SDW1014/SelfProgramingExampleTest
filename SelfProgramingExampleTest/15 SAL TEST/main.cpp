// title : _In_ 주석을 사용하여 인자가 null이 아니어야 함을 명시
// explanation : _In_ 주석을 사용하여 인자가 null이 아니어야 함을 명시
// author : SDW
// date : 2024-07-09

// header =================================================================================
#include <sal.h>
#include <iostream>
// header =================================================================================

// global function ========================================================================
// _In_ 주석을 사용하여 인자가 null이 아니어야 함을 명시
void PrintNumber(_In_ const int* pNumber)
{
	std::cout << "Number : " << *pNumber << std::endl;
}
// global function ========================================================================

// entry point ============================================================================
int main()
{
	int number = 42;
	int* pNumber = &number;

	// 유효한 포인터를 전달
	PrintNumber(pNumber);

	int* pNull = nullptr;

	// null 포인터를 전달 (정적 분석 도구가 경고를 표시할 것) 
	PrintNumber(pNull); //null이 아니어야 한다고 경고 중

	// Debug 모드 : 프로그램이 멈추고 경고를 표시
	// Release 모드 : 경고를 표시하고 계속 진행

	// 경고를 무시하고 싶다면
	// _In_opt_ 를 사용하면 경고가 사라짐

	// 사용에 대한 설명
	// _In_ : 인자가 null이 아니어야 함을 명시
	// _In_opt_ : 인자가 null일 수도 있음을 명시
	// _Out_ : 인자가 null이 아니어야 함을 명시
	// _Out_opt_ : 인자가 null일 수도 있음을 명시

	return 0;
}
// entry point ============================================================================